---
import ExperienceItem from "./ExperienceItem.astro";

const PROJECTS = [
  //Poner los más nuevos primero - Actualizar con datos reales - Si se agrega enlaces a proyectos, tambien se puede poner a github

  {
    title: "Project 1",
    description:
      "Lorem ipsum.... Por que no me lo pone por defecto? Bueno, ya fue....",
    link: "#",
    github: "#",
    image: "https://via.placeholder.com/300x200",
    tags: ["HTML", "CSS", "JavaScript"],
  },
];
---

{
  PROJECTS.map(({ title, description, link, github, image, tags }) => (
    <article class="">
      <h3 class="text-2xl font-semibold text-violet-400 mb-2">{title}</h3>
      <p class="text-lg text-pretty">{description}</p>
      <ul>
        {tags.map((tag) => (
          <li>{tag}</li>
        ))}
      </ul>
      <img class="rounded shadow-2xl shadow-white/10" src={image} alt={`Vista previa del proyecto ${title}`} />
    </article>
  ))
}
